---
description: Get all 16 personality types' opinions on a topic, then synthesize. Usage: /minds [--mode=light|middle|heavy] [--types=a,b,...] [--n=N] [--save] [--out=<path>] <topic>
---

ユーザーが `/minds` を実行しました。

引数: $ARGUMENTS

## 引数の解釈

以下のフラグを取り出し、残りを「お題」とする。

- `--mode=light|middle|heavy`（`--mode light` 形式も可）: 既定 `middle`
- `--types=intj,enfp,...`: カンマ区切りで対象タイプを限定。指定がなければ全16タイプ。
- `--n=N`: 数値。`--types` 未指定時は16タイプから先頭N個（一覧順）を使う。`--types` 指定時は `--n` は無視する。
- `--save`: 実行結果を `Outputs/<timestamp>-minds-<mode>.md` に保存する。
- `--out=<path>`: 保存先を明示指定（`--save` を暗黙的に有効化）。

利用可能なタイプ一覧（一覧順）:
`intj, intp, entj, entp, infj, infp, enfj, enfp, istj, isfj, estj, esfj, istp, isfp, estp, esfp`

## エラー処理

- お題が空の場合:
  ```
  Usage: /minds [--mode=light|middle|heavy] [--types=a,b,...] [--n=N] [--save] [--out=<path>] <topic>
  Example: /minds AIによるコードレビューの是非
  Example: /minds --mode=heavy --types=intj,enfp,istp このアーキテクチャ案
  Example: /minds --save Qiita記事のタイトル候補
  ```
- 未知のタイプ名が `--types` に含まれる場合は、その旨を伝えて利用可能タイプを返して終了。
- モード名が不正な場合も同様に終了。

## 実行フロー

モードによって段階が変わる。**フェーズ間で対象agent群への呼び出しは必ず並列（同一メッセージ内で複数のTaskツール呼び出し）で行うこと。**

### Phase 1（全モード共通）: 全タイプの light パス

対象タイプ全員（既定16）を **並列に** Task ツールで呼び出す。各 subagent には、お題と light モード出力フォーマット指定を渡す。

light モード出力フォーマット指定（subagentに渡す本文）:
```
お題に対し、以下の4行のみで答えてください。挨拶・前置き・まとめは一切不要です。

score: <1〜5の整数。あなたの価値観から見た総合評価。5=完全賛成、3=条件付き、1=完全反対>
stance: <一行の立場宣言>
key_axis: <判断に最も効いたあなたの価値観の軸（あなたの人格定義に出てくる言葉で）>
concern: <あなたの価値観から見た最大の懸念を一行>
```

全員の結果が出揃ったら、見やすい表として整形して表示する：

```
### 全タイプの一次評価

| type | score | stance | key_axis | concern |
|---|---|---|---|---|
| intj | 4 | ... | ... | ... |
...
```

### Phase 2（middle / heavy のみ）: 発散意見の深掘り

Phase 1 の結果を見て、**意見が割れた／極端だった／他と異なる視点を持っていた** タイプを 3〜5 個選ぶ。選定基準は以下のいずれか：

- score が分布の両端（最も賛成・最も反対）
- key_axis が他と被っていない独自の軸を提示している
- concern が他のタイプが見落としている視点を含む

選んだタイプ群を **並列に** Task ツールで再度呼び出し、middle モード出力フォーマットで深掘りさせる。

middle モード出力フォーマット指定（subagentに渡す本文）:
```
お題に対し、以下の構成で約150〜250字で答えてください。あなたの口調を必ず保ってください。挨拶・前置き・まとめは不要です。

stance: <一行の立場宣言>
reasoning: <あなたの価値観からその立場に至る筋道を2〜3文で。あなたの口調で>
watchpoint: <あなたの価値観から見た見落としやすい点・推しポイント・引っかかりを1〜2文で>
```

結果を以下のように表示：
```
### 深掘り（middle）

#### <TYPE>
<出力をそのまま>
```

### Phase 3（middle / heavy）: 合議・統合

Phase 1 + Phase 2 の全出力を踏まえ、ユーザー（中立な書き手）の口調で以下を統合する。subagent は呼ばず、メインの assistant が直接書く。**以下の4セクションは必須**（省略しない）。発散を避けるため、各セクションのフォーマット要件は厳守すること。

```
### 合議

#### score 分布
<最低スコア・最頻スコア・最高スコアを一文で。例: 「score=2〜5、中央値4、最高はESTP(5)、最低はINTPとINFP(3)」>

#### 主要な対立軸（2〜3個）
各軸を以下の形式で：
- **<軸名>**: <一方の立場>（代表: <タイプ群>）vs <他方の立場>（代表: <タイプ群>）
- ...

#### 見落とされがちな視点（1〜3個）
各点を以下の形式で：
- **<視点の名前>** (出典: <タイプ>): <なぜ他のタイプが見落とすのか、何が独自なのかを1〜2文で>
- ...

#### 落としどころ候補（2〜3案）
各案を以下の形式で：
- **案<N>**: <タイトルまたは一行の方針>
  - 賛同しそうなタイプ: <タイプ群>
  - 適性: <この案が噛み合うシチュエーション・前提条件>
  - リスク: <この案が取りこぼす視点／崩れる条件>

最後に1〜2文で、どの案がデフォルト推奨か（理由付きで）を明示する。
```

### Phase 4（heavy のみ）: 反論ラウンド

Phase 2 で深掘りした3〜5タイプを **並列に** 再呼び出しする。各 subagent には、自分以外の Phase 2 出力を提示し、「最も自分の立場と相容れない他者意見1つを選び、それに対する反論を100〜200字で」と指示する。

反論ラウンド出力フォーマット指定（subagentに渡す本文の核）:
```
以下は別人格による意見です。最もあなたの立場と相容れない意見を1つだけ選び、
そのタイプ名を冒頭に「target: <type>」として明記したうえで、
あなたの口調と価値観で100〜200字以内の反論を書いてください。
挨拶や前置きは不要です。

[ここに Phase 2 の他者出力をすべて並べる]
```

結果を以下のように表示：
```
### 反論ラウンド（heavy）

#### <TYPE> → target: <other>
<反論本文>
```

その後、Phase 3 の合議パートを **書き直す**（反論を踏まえてより練られたものに）。書き直した版は `### 合議（最終）` という見出しで提示し、Phase 3 と同じ4セクション構造を厳守する。

## 出力構成のまとめ

- light: Phase 1 の表のみ
- middle: Phase 1 表 + Phase 2 深掘り + Phase 3 合議
- heavy: Phase 1 表 + Phase 2 深掘り + Phase 3 合議 + Phase 4 反論ラウンド + Phase 3 を書き直した「合議（最終）」

## 保存（`--save` / `--out=<path>` 指定時）

実行が完了した後、**画面に表示した内容と同じものをファイルに書き出す**。

保存先パス決定:
- `--out=<path>` が指定されていればそのパスをそのまま使う（ディレクトリパスのみなら自動でファイル名を組み立てる）。
- `--save` のみなら `Outputs/<YYYYMMDD-HHMMSS>-minds-<mode>.md`（CWD 直下の `Outputs/` ディレクトリ。存在しなければ作成する）。

ファイル内容の構造：

```
# /minds 実行ログ

- 日時: <ISO 8601>
- モード: <light|middle|heavy>
- 対象タイプ: <カンマ区切り、または「全16タイプ」>
- お題: <ユーザーが渡したお題そのまま>

---

(以下、Phase 1 の表 → Phase 2 深掘り → Phase 3 合議 → Phase 4 反論ラウンド → 合議（最終）の順に、画面表示と同じ内容を貼る)
```

保存が完了したらユーザーに `保存しました: <絶対パス>` と1行で報告する。

## 注意

- subagent への呼び出しは必ず並列で行う。逐次呼び出しはレイテンシを大きく悪化させる。
- 各 subagent には「お題」と「出力フォーマット指定」だけを渡す。他のタイプの出力は Phase 4 を除き渡さない（独立した一次意見を保つため）。
- subagent の出力は加工せずに表示する。要約や言い換えはしない。
- Phase 3（および Phase 4 後の「合議（最終）」）は必ず4セクション構成を守る。発散を防ぐためのルブリックなので省略・改変しない。
